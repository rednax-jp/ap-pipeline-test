pipeline {
  agent any
  stages {
    stage('test') {
      steps {
        echo "testing"
        sh "docker version"
      }
    }
    stage('test') {
          steps {
            sh "echo testing"
            sh "docker version"
          }
        }
  }
}